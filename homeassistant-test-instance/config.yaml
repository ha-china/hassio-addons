name: "Home Assistant Test Instance"
version: "0.2.7"
slug: "ha_test_instance"
image: ghcr.1ms.run/faserf/hassio-addons-homeassistant-test-instance-{arch}
description: "[老王杂谈说]：一个用于测试目的的独立 Home Assistant Core 实例。"
url: "https://gh-proxy.org/github.com/home-assistant/home-assistant"
arch:
  - aarch64
  - amd64
ports:
  8123/tcp: 8124
map:
  - homeassistant_config:rw
ingress: true
ingress_port: 8123
init: false
options:
  log_level: "info"
schema:
  log_level: list(trace|debug|info|warning|error|fatal)
stage: experimental
